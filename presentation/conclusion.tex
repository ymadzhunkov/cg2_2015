
\begin{frame}[fragile]
\frametitle{Спънки}
\pause
\begin{lstlisting}
RandomGenerator rng;
for (int i = 0; i < nSamples; i++) {
   x   = sample(rng, ..);
   res = compute(x, ..);
   accumulate(res);
}
reportResults();
\end{lstlisting}
\end{frame}

\begin{frame}
  \frametitle{Въпроси} \pause
     
\end{frame}
